#include <iostream>
#include <string>
#include <cmath>

/*Simplifique el siguiente código:

for (sum = i = 0, j = 2, k = i + j; i < 10 || k < 15; ++i, ++j, ++k) {
    sum += (i < j)? k : i;
}

Recuerde que las expresiones entre comas se evaluan de izquierda a derecha, con cada evaluación entre comas evaluada en orden estricto.
*/

int main(void){
    return EXIT_SUCCESS;
}